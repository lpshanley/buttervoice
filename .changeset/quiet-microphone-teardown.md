---
"buttervoice": patch
---

Stop microphone capture before releasing recording and permission-check streams so the macOS microphone indicator clears when Microphone Buffer is off. Reuse the system default microphone handle when that microphone is selected by name.
